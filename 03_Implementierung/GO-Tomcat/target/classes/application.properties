# DataSource settings: set here your own configurations for the database
# connection. In this example we have "netgloo_blog" as database name and
# "root" as username and password.
spring.datasource.url=jdbc:mysql://localhost/PSESoSe17Gruppe3?useSSL=false
spring.datasource.username=PSESoSe17User3
spring.datasource.password=vAprU3acuxux3S8a
# spring.datasource.url=jdbc:mysql://localhost/pse_development?useSSL=false
# spring.datasource.username=root
# spring.datasource.password=69h97jnv
# Keep the connection alive if idle for a long time (needed in production)
# spring.datasource.testWhileIdle = true
# spring.datasource.validationQuery = SELECT 1
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
# Naming strategy
# spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
#specify logging behaviour
logging.config=classpath:logback-spring.xml
logging.file=logs/go-logfile.log